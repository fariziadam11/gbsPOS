# Server
PORT=8081
ENV=development

# Database
DATABASE_URL=postgres://gbspos:gbspos_secret@localhost:5432/gbs_pos?sslmode=disable
# Set MIGRATIONS_PATH to use golang-migrate (production). Leave empty for AutoMigrate (dev/test).
MIGRATIONS_PATH=

# JWT (only required when ENABLE_DEMO_AUTH=true)
JWT_SECRET=your-super-secret-jwt-key-minimum-32-characters
JWT_EXPIRY_HOURS=24

# Keycloak
KEYCLOAK_BASE_URL=https://auth.armmada.id
KEYCLOAK_REALM=gbs-pos
ENABLE_DEMO_AUTH=false

# Logging
LOG_LEVEL=debug

# CMS Upload Directory
UPLOAD_DIR=./uploads/ads
